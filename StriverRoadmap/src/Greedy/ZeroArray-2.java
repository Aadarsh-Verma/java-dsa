package Greedy;

import java.util.*;

public class ZeroArray {
    /* Q Any two elements can be picked and reduced by 1 determine if the entire array can be reduced to 0
       int main()
{
    ios_base::sync_with_stdio(false);
    cin>>n;
    for (ll i=1; i<=n; i++)
    {
        cin>>a;
        s+=a;
        m=max(m, a);
    }
    if (s%2==1 || s<2*m)
    {
        cout<<"NO";
        return 0;
    }
    cout<<"YES";
    return 0;
}
    * */
    public static void main(String[] args) {

    }
}
